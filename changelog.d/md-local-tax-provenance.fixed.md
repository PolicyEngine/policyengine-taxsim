Report the build of 2026 TAXSIM binaries, which stamp `cd2026081819` rather than `cdate-...`, and pin Maryland `siitax` to TAXSIM's state-only output. The single-household path used for `--logs` YAML tests now zeroes Maryland county tax as the batch path does. A new test flags any bundled binary that runs TAXSIM's Maryland county-tax block, as build cd2026081819 does.
