Keep every emulator override (SALT for `--disable-salt`, QBID W-2 wages, the rental QBID gate, MN CRP, state SSI-supplement zeroing, MD local-tax zeroing, ME rent utilities, NY separate payments) in the wage-perturbation branch that computes `frate`/`srate`. The branch had dropped them, so Maryland `srate` included the county tax the emulator zeroes (2253.01 instead of 4.75 for $80k single in 2024), and rental-income and `--disable-salt` itemizer `frate` values went far negative.
